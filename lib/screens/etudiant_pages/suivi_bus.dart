import 'package:flutter/material.dart';

class SuiviBusPage extends StatelessWidget {
  const SuiviBusPage({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Page de suivi du bus',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}